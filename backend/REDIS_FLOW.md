# Redis Cache Flow - Dự án Girl Pick

## 📊 Tổng quan Flow

```
Client Request → Backend API → Cache Check → [Cache Hit/Miss] → Response
                                      ↓
                              [Cache Miss] → Database Query → Save to Cache → Response
```

---

## 🔄 Flow chi tiết cho từng API

### 1. **GET /girls** - Lấy danh sách girls

#### Flow hoạt động:

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. Client gửi request: GET /girls?page=1&limit=20&province=SG  │
└──────────────────────────────┬──────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│ 2. GirlsController.findAll() nhận request                      │
│    → Gọi GirlsService.findAll(filters)                         │
└──────────────────────────────┬──────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│ 3. GirlsService.findAll()                                     │
│    → Generate Cache Key:                                       │
│      "girls:list:1:20::0:::::::SG"                            │
│      (Format: prefix:page:limit:filters...)                    │
└──────────────────────────────┬──────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│ 4. CacheService.get(cacheKey)                                 │
│    → Gọi Redis: GET "girls:list:1:20::0:::::::SG"            │
└──────────────────────────────┬──────────────────────────────────┘
                               ↓
                    ┌──────────┴──────────┐
                    │                     │
            ┌───────▼──────┐      ┌───────▼──────┐
            │  CACHE HIT  │      │  CACHE MISS   │
            └───────┬──────┘      └───────┬──────┘
                    │                     │
                    │                     ↓
                    │      ┌──────────────────────────────┐
                    │      │ 5. Query Database            │
                    │      │    Prisma.girl.findMany()    │
                    │      │    + Prisma.girl.count()     │
                    │      └──────────────┬───────────────┘
                    │                     │
                    │                     ↓
                    │      ┌──────────────────────────────┐
                    │      │ 6. Save to Redis Cache      │
                    │      │    CacheService.set(         │
                    │      │      key,                    │
                    │      │      result,                 │
                    │      │      ttl: 300-600s           │
                    │      │    )                         │
                    │      └──────────────┬───────────────┘
                    │                     │
                    └──────────┬──────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│ 7. Return response to client                                    │
│    Response time:                                               │
│    - Cache Hit:  ~1-5ms    (từ Redis)                         │
│    - Cache Miss: ~200-500ms (từ Database)                      │
└─────────────────────────────────────────────────────────────────┘
```

#### Cache Key Format:
```
girls:list:{page}:{limit}:{districts}:{rating}:{verification}:{isFeatured}:{isPremium}:{priceFilter}:{ageFilter}:{heightFilter}:{weightFilter}:{originFilter}:{locationFilter}:{province}
```

**Ví dụ:**
- `girls:list:1:20::0:::::::SG` - Trang 1, 20 items/page, province=SG
- `girls:list:2:20::0:::::::SG` - Trang 2, cùng filter
- `girls:list:1:20::0:true::::::SG` - Trang 1, featured=true

#### TTL (Time To Live):
- **Trang 1, không filter**: 600 giây (10 phút) - Data quan trọng nhất
- **Có filter hoặc trang khác**: 300 giây (5 phút) - Data ít được truy cập hơn

---

### 2. **GET /girls/:id** - Lấy chi tiết một girl

#### Flow hoạt động:

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. Client gửi request: GET /girls/abc123                      │
└──────────────────────────────┬──────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│ 2. GirlsController.findOne(id)                                 │
│    → Gọi GirlsService.findOne(idOrSlug, incrementView=true)     │
└──────────────────────────────┬──────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│ 3. GirlsService.findOne()                                      │
│    → Generate Cache Key:                                        │
│      "girls:detail:abc123"                                      │
└──────────────────────────────┬──────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│ 4. CacheService.get("girls:detail:abc123")                     │
│    → Gọi Redis: GET "girls:detail:abc123"                      │
└──────────────────────────────┬──────────────────────────────────┘
                               ↓
                    ┌──────────┴──────────┐
                    │                     │
            ┌───────▼──────┐      ┌───────▼──────┐
            │  CACHE HIT   │      │  CACHE MISS   │
            └───────┬──────┘      └───────┬──────┘
                    │                     │
                    │                     ↓
                    │      ┌──────────────────────────────┐
                    │      │ 5. Query Database            │
                    │      │    Prisma.girl.findFirst()   │
                    │      │    với full relations        │
                    │      └──────────────┬───────────────┘
                    │                     │
                    │                     ↓
                    │      ┌──────────────────────────────┐
                    │      │ 6. Increment View Count     │
                    │      │    (async, không block)     │
                    │      └──────────────┬───────────────┘
                    │                     │
                    │                     ↓
                    │      ┌──────────────────────────────┐
                    │      │ 7. Save to Redis Cache      │
                    │      │    TTL: 600s (10 phút)      │
                    │      └──────────────┬───────────────┘
                    │                     │
                    └──────────┬──────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│ 8. Return response to client                                    │
└─────────────────────────────────────────────────────────────────┘
```

#### Cache Key Format:
```
girls:detail:{idOrSlug}
```

**Ví dụ:**
- `girls:detail:abc123` - Girl với ID abc123
- `girls:detail:girl-xinh-dep` - Girl với slug "girl-xinh-dep"

#### TTL:
- **600 giây (10 phút)** - Chi tiết ít thay đổi, cache lâu hơn

---

### 3. **POST /girls** - Tạo girl mới

#### Flow hoạt động:

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. Client gửi request: POST /girls (Admin/Staff)               │
└──────────────────────────────┬──────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│ 2. GirlsService.create()                                       │
│    → Insert vào Database                                        │
└──────────────────────────────┬──────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│ 3. Invalidate Cache                                            │
│    → invalidateListCache()                                      │
│      - Tăng version number: "girls:list:version"               │
│      - Tất cả list cache sẽ không match version mới            │
└─────────────────────────────────────────────────────────────────┘
```

**Lý do:** Khi có girl mới, danh sách cần được refresh để hiển thị girl mới.

---

### 4. **PATCH /girls/:id** - Cập nhật girl

#### Flow hoạt động:

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. Client gửi request: PATCH /girls/abc123                    │
└──────────────────────────────┬──────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│ 2. GirlsService.updateById() hoặc update()                     │
│    → Update Database                                            │
└──────────────────────────────┬──────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│ 3. Invalidate Cache                                            │
│    → invalidateGirlCache(id)                                    │
│      - Xóa: "girls:detail:abc123"                              │
│    → invalidateListCache()                                      │
│      - Tăng version number                                     │
└─────────────────────────────────────────────────────────────────┘
```

**Lý do:** 
- Detail cache cần xóa vì data đã thay đổi
- List cache cần xóa vì có thể ảnh hưởng đến sorting/filtering

---

### 5. **DELETE /girls/:id** - Xóa girl

#### Flow hoạt động:

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. Client gửi request: DELETE /girls/abc123                    │
└──────────────────────────────┬──────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│ 2. GirlsService.remove()                                       │
│    → Delete từ Database                                        │
└──────────────────────────────┬──────────────────────────────────┘
                               ↓
┌─────────────────────────────────────────────────────────────────┐
│ 3. Invalidate Cache                                            │
│    → invalidateGirlCache(id)                                    │
│    → invalidateListCache()                                      │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🗝️ Cache Key Strategy

### Pattern:
```
{module}:{type}:{params...}
```

### Ví dụ thực tế:

#### List Cache:
```
girls:list:1:20::0:::::::SG
girls:list:1:20::0:true::::::SG
girls:list:2:20::0:::::::SG
```

#### Detail Cache:
```
girls:detail:abc123
girls:detail:girl-xinh-dep
```

#### Version Cache (cho invalidation):
```
girls:list:version  → 1, 2, 3...
```

---

## ⚡ Performance Benefits

### Response Time Comparison:

| Scenario | Without Cache | With Cache | Improvement |
|----------|--------------|------------|-------------|
| First request (cache miss) | 200-500ms | 200-500ms | Same |
| Subsequent requests (cache hit) | 200-500ms | 1-5ms | **50-100x faster** |
| High traffic (1000 req/min) | Database overload | Redis handles | **No DB stress** |

### Memory Usage:
- **Redis**: ~10-50MB cho 1000 cached items
- **Database**: Không bị query liên tục

---

## 🔧 Cache Invalidation Strategy

### Khi nào cache bị invalidate?

1. **Create Girl** → Invalidate list cache
2. **Update Girl** → Invalidate detail + list cache
3. **Delete Girl** → Invalidate detail + list cache
4. **Update Images** → Invalidate detail cache

### Version-based Invalidation:

Thay vì xóa tất cả keys (tốn thời gian), dùng version number:

```typescript
// Khi invalidate
version = version + 1  // Tăng version

// Khi check cache (có thể implement sau)
if (cachedVersion < currentVersion) {
  // Cache outdated, fetch fresh data
}
```

---

## 📈 Monitoring & Debugging

### Xem cache trong Redis:

```bash
# Connect to Redis
docker exec -it redis redis-cli

# List all cache keys
KEYS girls:*

# Get specific cache
GET "girls:detail:abc123"

# Check TTL
TTL "girls:detail:abc123"

# View cache stats
INFO stats
```

### Log trong Backend:

```
[GirlsService] Cache hit for: girls:list:1:20::0:::::::SG
[GirlsService] Cache miss, fetching from database...
[GirlsService] Invalidated cache for girl: abc123
[GirlsService] Invalidated list cache, new version: 2
```

---

## 🚨 Error Handling

### Redis Connection Failed:

- Backend sẽ **fallback về Database**
- Website vẫn hoạt động bình thường (chỉ chậm hơn)
- Log warning để admin biết

### Cache Miss:

- Tự động query database
- Save vào cache cho lần sau
- Không ảnh hưởng đến user

---

## 🎯 Best Practices Đã Áp Dụng

1. ✅ **Cache Key có structure rõ ràng**: `module:type:params`
2. ✅ **TTL hợp lý**: 5-10 phút tùy loại data
3. ✅ **Auto invalidation**: Khi data thay đổi
4. ✅ **Fallback mechanism**: Nếu Redis down, vẫn dùng DB
5. ✅ **Version-based invalidation**: Hiệu quả hơn pattern matching

---

## 🔮 Future Improvements

1. **Cache warming**: Pre-load popular data khi server start
2. **Cache statistics**: Track hit/miss ratio
3. **Smart TTL**: Tự động điều chỉnh TTL dựa trên access frequency
4. **Distributed cache**: Nếu scale multi-server
5. **Cache compression**: Compress large objects trước khi cache

